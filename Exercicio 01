#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
#include <math.h>

int main()
{
    setlocale(LC_ALL, "Portuguese");

    int x, r, i;

    printf("Entre com um valor: ");
    scanf("%d",&x);

    for (i=0; i<=10; i++){
        r = pow(x,i);
        printf("%d \n",r);
    }
}
